# AI 读书笔记库 — 设计规约（Design Spec）

> 文档定位：本文规定"做什么、为什么、边界在哪"，技术选型（语言/框架/具体库）作为开放项留给实现者。
> 0 为**硬约束**，落地时不可妥协。

---

## 0 三条核心原则（硬约束）

| 编号 | 原则 | 含义 | 否决依据 |
|------|------|------|----------|
| **P1** | **AI 只生成，不治理** | AI 负责"阅读、总结、撰写"等创造性产出；一切"移动/删除/重命名/索引/同步"等确定性与破坏性操作由脚本完成 | 若有人提议"让 AI 直接管理整个目录"，凭此条否决 |
| **P2** | **单一事实源（SSOT）** | `./1notes` 是笔记的唯一权威来源；Kavita 是只读书源，SiYuan 是只写下游 | 禁止任何双向同步 |
| **P3** | **可追溯、可回滚** | 全库纳入 Git；所有自动操作可 diff、可还原；AI 不得执行不可逆操作 | 无版本保护的写操作一律拒绝 |

**心智模型**：不是"把目录交给 AI 管理"，而是"AI 在受约束规则下生成产物，由确定性脚本 + 版本控制来治理"。

---

## 1 数据流向架构

```
[Kavita 书源]  →  [./1books]  →  [AI 分析]  →  [./1notes/*.md]  →  [SiYuan]
   只读            工作副本        生成           本地事实源(SSOT)    只写/展示
```

- 流向**单向**，禁止逆流。
- 唯一事实源 = `./1notes`（md + Git，可 diff/可回滚）。
- SiYuan 的块数据库是其私有结构，**不作为源头**。

---

## 2 目录结构规约

```
library/
├── 0meta/                    # 系统元数据（机器读，非给人看）
│   ├── index.json            # 系统大脑：书↔笔记映射、状态、哈希
│   └── prompts/              # 提示词模板（版本化，可回溯）
├── 1books/                   # 书源工作副本
│   └── <book-id>/            # 每书一目录，规避文件名冲突
│       └── source.epub
├── 1notes/                   # 笔记 SSOT
│   └── <book-id>/
│       ├── summary.md        # 全书总览
│       ├── chapters/         # 分章笔记
│       └── highlights.md     # 摘录与金句
├── 9scripts/                 # 确定性治理脚本
└── README.md                 # 自动生成索引（禁止手改）
```

**关键约定**
- **目录键用 `<book-id>`，不用书名。** 书名含空格/特殊符/多语言，做路径易错。
  `book-id = slug(书名) + "-" + shorthash`，例：`thinking-fast-slow-a1b2c3`。
- `0meta/` 不给人直接编辑，由脚本读写。

---

## 3 `index.json` 字段设计（系统大脑）

```jsonc
{
  "schema_version": 1,
  "books": [
    {
      "book_id": "thinking-fast-slow-a1b2c3",
      "title": "思考，快与慢",
      "author": "Daniel Kahneman",
      "source": {
        "path": "1books/thinking-fast-slow-a1b2c3/source.epub",
        "format": "epub",
        "sha256": "……",          // 判断书是否变更→是否需重跑
        "kavita_series_id": null   // 可选：与 Kavita 关联
      },
      "notes": {
        "dir": "1notes/thinking-fast-slow-a1b2c3",
        "summary": "summary.md"
      },
      "status": "done",            // 见 4 状态机
      "tags": ["心理学", "决策"],
      "siyuan_doc_id": null,       // 同步幂等用
      "created_at": "2025-01-01T00:00:00Z",
      "updated_at": "2025-01-01T00:00:00Z",
      "last_error": null
    }
  ]
}
```

**幂等关键**：`source.sha256` 变化 → 触发重新分析；`siyuan_doc_id` 存在 → 同步走"更新"而非"新建"。

---

## 4 处理状态机

```
pending ──→ analyzing ──→ drafted ──→ reviewed ──→ synced
   │            │            │            │
   └──→ failed ←┘            │            │
                            （强制人工抽检关卡）
```

| 状态 | 含义 | 谁推进 |
|------|------|--------|
| `pending` | 已入库，待分析 | 脚本（扫描新书） |
| `analyzing` | AI 处理中 | 脚本调度 |
| `drafted` | AI 已产出 md 草稿 | AI |
| `reviewed` | **人工抽检通过** | 人（硬关卡，不可自动跳过） |
| `synced` | 已推送 SiYuan | 脚本 |
| `failed` | 出错，记 `last_error` | 脚本 |

> `reviewed` 为强制人工关卡：在你尚未确认 AI 总结质量前，不允许自动流入 SiYuan，避免污染知识库。

---

## 5 模块职责矩阵（权限边界）

| 交给 AI（生成/创造） | 交给脚本（确定/破坏/全局一致） |
|---|---|
| 阅读 epub/txt 并总结 | 文件移动/删除/重命名 |
| 撰写章节笔记、提炼观点 | 生成/更新 README |
| 建议 frontmatter 字段 | 计算哈希、判断是否需重跑 |
| 调 SiYuan API 写入正文 | Git commit / 备份 / 状态机推进 |

**原则**：凡"不可逆"或"需全局一致性"的操作，一律不交给 AI。

---

## 6 AI 长文本处理规约（Map-Reduce 两段式）

epub 常达十余万字，超单次上下文，必须分块；但分块会丢全局脉络，故采用两段式：

1. **Map（分块精炼）**：按章/按固定 token 窗口切分，每块产出
   - 该块要点（bullet）
   - 关键摘录（带原文定位）
   - 该块在论证链中的作用
2. **Reduce（全局缝合）**：将各块的"作用描述"汇总，由 AI 重建
   - 全书论证主线 / 结构脉络
   - 章节间呼应、伏笔、递进
   - 最终 `summary.md`

**约束**
- 切分点优先落在章节边界，减少语义割裂。
- 每块保留**前序摘要（rolling summary）**作为上下文，缓解脉络断裂。
- 提示词模板存 `0meta/prompts/` 并版本化，便于复现与回溯。

---

## 7 README 自动生成规约

- README **禁止人工/AI 手写**，由脚本遍历 `1notes/` 读取 frontmatter 渲染。
- 每篇笔记顶部 YAML frontmatter：

```yaml
---
title: 思考，快与慢
author: Daniel Kahneman
book_id: thinking-fast-slow-a1b2c3
status: done
tags: [心理学, 决策]
created: 2025-01-01
---
```

- 脚本据此生成可点击链接表（书名 | 作者 | 标签 | 状态 | 链接），保证 100% 准确、无漏写、无坏链。

---

## 8 SiYuan 单向同步规约

- 通过 SiYuan **Kernel API（本地 HTTP）** 写入指定笔记本，正文从磁盘读、不经 LLM 上下文。
- 仅在状态 = `reviewed` 后推送。
- **嵌套树镜像本地多文档结构**（非缝合成单篇）：父文档（书信息+epub附件+标签+分类）下挂 `全书总览 / 摘录与金句 / 分章笔记`，`分章笔记` 下**每章一独立文档**，顺序由内核 `changeSort` 锁定。
- **文档间互链用块引用 `((id))`**（双向，进反向链接面板/关系图）：父↔子、章↔章（上/下一章）、目录↔各章；让知识库可在树形浏览与图谱漫游间切换。
- **分类/选书**：标签取自 `summary` frontmatter `tags`，以 `#标签#` 写入父文档供标签面板筛选；大类取 `category`，聚合为顶层「读书馆」选书页（每书一块引用）。
- **幂等**：父 `siyuan_doc_id` 为空→新建并回写 id；非空→先删旧父子树（级联）再重建，重复推送不产生重复文档。读书馆 id 存 `index.json` 顶层 `siyuan_shelf_id`。
- **单向**：不从 SiYuan 拉回。个人批注请放 SiYuan 独立子块，与同步内容物理隔离。

---

## 9 Kavita 集成规约

- Kavita 提供 OPDS / REST API，但其强项是**阅读管理而非文件交换**。
- 方案：**Kavita 保留为阅读入口；分析用副本独立放 `1books/`，二者解耦。**
- 避免直接让脚本干预 Kavita 库目录，防止其扫描机制与本系统互相干扰。
- 可选：在 `index.json` 记 `kavita_series_id` 做弱关联。

---

## 10 落地路线（按"假设验证"排序）

> 顺序即风险递减顺序。**A 不通过则全停**，不得跳级。

| 阶段 | 目标 | 通过标准 |
|------|------|----------|
| **A. 质量验证** | 手动跑 3–5 本：epub→AI 总结→md→人工评估 | 笔记质量达你预期，否则不值得自动化 |
| **B. 单本管线** | 跑通单本：分块→Map-Reduce→生成 md + frontmatter | 单本全自动产出可用草稿 |
| **C. 治理层** | `index.json` + 状态机 + README 自动生成 | 批量可追踪、索引零错误 |
| **D. SiYuan 同步** | 单向幂等推送 | 重复推送不产生重复文档 |
| **E. Kavita 关联** | 弱集成书源 | 不干扰 Kavita 正常使用 |

---

## 11 风险登记册

| 风险 | 说明 | 缓解 |
|------|------|------|
| 过度工程化 | 藏书少时，状态机+脚本是杀鸡用牛刀 | 先做 A/B，规模上来再上 C |
| 总结质量天花板 | 分块损失全局脉络，LLM 固有局限 | Map-Reduce + rolling summary，仍需人工抽检 |
| 三系统耦合脆弱 | 本地+Kavita+SiYuan，任一 API 变更即断链 | 解耦、单向、幂等；每环可独立降级 |
| AI 越权 | 误删/误改不可逆 | P1+P3：写操作全归脚本 + Git |

---

## 12 开放技术选型项（留给实现者）

- 实现语言与运行时
- epub/txt 解析库
- 分块策略的具体 token 阈值
- LLM 供应商与上下文窗口
- Git 自动化方式（钩子/定时/手动）
- 脚本调度（CLI / 定时任务 / 文件监听）

> 以上可自由决策，**但不得违反 0**。

---

## 13 交付校验清单（验收标准）

- [ ] 全库纳入 Git，写操作可回滚
- [ ] AI 无任何文件删除/移动权限
- [ ] `index.json` 能正确反映每本书状态与哈希
- [ ] README 由脚本生成，链接零错误
- [ ] 单本可跑通 Map-Reduce 并产出带 frontmatter 的 md
- [ ] SiYuan 重复推送不产生重复文档
- [ ] 存在强制人工抽检关卡（reviewed）

---

### 附录 A：book-id 生成规则
`slug(title)` 取小写、空格转连字符、去特殊符；追加 `source.sha256` 前 6 位。

### 附录 B：提示词版本化
`0meta/prompts/` 下每个模板带版本号；`index.json` 记录每本书所用模板版本，确保结果可复现。